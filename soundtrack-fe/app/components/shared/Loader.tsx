const Loader = () => (
  <span className="relative w-[16px] h-[16px] rounded bg-white shadow-[32px 0 #fff, -32px 0 #fff] animate-[flash 0.5s ease-out infinite alternate]" />
);

export default Loader;
